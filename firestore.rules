rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sites/activity-plan {
      allow read, write: if true;
    }
    match /attendance/shared {
      allow read, write: if true;
    }
  }
}
